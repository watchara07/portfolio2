/* style.css */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Sarabun', sans-serif; /* ใช้ฟอนต์ไทยที่ดูทันสมัย */
}

body {
    background-color: #f0f0f0; /* สีพื้นหลังเทาอ่อนมาก */
    color: #333;
    line-height: 1.6;
}

/* แถบเมนูด้านบน */
header {
    background-color: #ffffff; /* แถบเมนูสีขาว */
    padding: 1rem 0;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    position: sticky;
    top: 0;
    width: 100%;
}

nav {
    display: flex;
    justify-content: center;
}

nav a {
    text-decoration: none;
    color: #555;
    margin: 0 15px;
    font-weight: bold;
    transition: 0.3s;
}

nav a:hover {
    color: #000;
    border-bottom: 2px solid #888;
}

/* ส่วนเนื้อหา */
.container {
    max-width: 800px;
    margin: 50px auto;
    background: #fff; /* กล่องเนื้อหาสีขาวเพื่อให้เด่นออกมาจากพื้นเทา */
    padding: 40px;
    border-radius: 15px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.05);
}

h1 {
    margin-bottom: 20px;
    color: #444;
    border-left: 5px solid #ccc;
    padding-left: 15px;
}

p {
    margin-bottom: 15px;
}

footer {
    text-align: center;
    padding: 20px;
    font-size: 0.8rem;
    color: #888;
}
